<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Proyecto-Final</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
    <link rel="shortcut icon" href="/favicon.ico" type="image/x-icon">
</head>

<body class="container-fluid bg-ligth d-flex flex-column flex-grow-1 align-middle justify-content-center">
    <div class="container my-1 d-flex flex-column sticky-top bg-dark p-2 rounded">
        <span
            class="d-sm-inline-flex text-center align-middle justify-content-center text-light fs-4 fw-bold opacity-75">
            Back-end Final Project </span>
    </div>
    <div class="container my-1 d-flex flex-row sticky-top bg-dark p-2 rounded justify-content-between">
        {{#if user.name}}
        <div>
            <a href="/users" class="btn btn-secondary btn-sm px-2 fw-bold" tabindex="-1" role="button"
                aria-disabled="true">BACK HOME</a>
        </div>
        <div>
            <a href="/users/logout" class="btn btn-danger btn-sm px-2 fw-bold" tabindex="-1" role="button"
                aria-disabled="true">LOGOUT</a>
        </div>
        <div class="btn btn-info btn-sm fw-bold">
            <a href="/users/profile" style=" text-decoration: none">
                <span style="color: rgb(32, 30, 30);">🧔🏻‍♂️ {{user.roll}}: {{user.name}}</span>
            </a>
        </div>
        {{else}}
        <div class="btn btn-warning btn-sm fw-bold">
            <a href="/users" class="btn btn-warning btn-sm p-0 fw-bold" tabindex="-1" role="button"
                aria-disabled="true">BACK HOME</a>
        </div>
        {{/if}}
    </div>
    </div>
    <div class="container-fluid absolute--fill flex-column h-100">
        {{{body}}}

    </div>
</body>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
    crossorigin="anonymous"></script>

</html>